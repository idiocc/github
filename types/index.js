export {}
/* typal types/github.xml namespace */
/**
 * @typedef {_idio.GithubExchangeResponse} GithubExchangeResponse `＠record` The response from GitHub for OAuth code exchange request.
 * @typedef {Object} _idio.GithubExchangeResponse `＠record` The response from GitHub for OAuth code exchange request.
 * @prop {string} error The error title.
 * @prop {string} error_description The error description.
 * @prop {string} access_token The received token.
 * @prop {string} scope The scope.
 * @prop {string} token_type The type, i.e. `bearer`.
 */
